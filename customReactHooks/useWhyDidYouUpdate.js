/**
 * Helps you debug why a component re-rendered
 *
 *
 */

/**
 * Input aspect:
 * 1. takes a name to debug
 * 2. props to inspect what changed
 *
 *
 * Output aspect:
 * 1. Nothing needed to return, we will internally log the reason to console
 */
/**
 * Behavior aspect
 *
 * 1. Will run an effect after each render
 * 2. Use a ref to hold previous props
 */
